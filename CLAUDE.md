# Embeddings Workshop

Learning-focused project. Experiments > production code.

## Conventions

- Scripts in `src/` — one file per experiment
- Keep experiments self-contained and runnable standalone
- Use Bun + TypeScript
- AWS Bedrock for model access (use `@aws-sdk/client-bedrock-runtime`)
